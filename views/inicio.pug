doctype html
html(lang="es")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title COIR ,  #{pagina}
        block styles
        link(rel="stylesheet", href="/css/app.css")
    body.min-h-screen.bg-gray-50
        
        header.bg-indigo-600.p-5
          .container.mx-auto.flex.justify-between.items-center
              a(href="/")
                  h1.text-2xl.text-white.font-extrabold.text-center PORTAL COIR
                      span.font-normal 
                  
              nav(class="my-5 text-sm md:flex md:items-center md:gap-3 font-bold text-white hidden")
                  a(href="/mis-servicios") Servicios
                  a(href="") Mi Perfil
                  form(action="/auth/cerrar-sesion" method="POST")
                      input(type="hidden" name="_csrf" value=csrfToken)
                      input( type="submit" class="bg-indigo-800 py-2 px-10 cursor-pointer" value="Cerrar Sesión")



        main(class="mx-auto container mt-10 px-2")
            block contenido

        block scripts